
  # Neura UI Design (Copy)2

  This is a code bundle for Neura UI Design (Copy)2. The original project is available at https://www.figma.com/design/QN7s3zvLeONn0koAlETrYB/Neura-UI-Design--Copy-2.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  